services:
  audiobookshelf:
    image: ghcr.io/scottdkey/audiobookshelf:latest
    container_name: audiobookshelf
    restart: unless-stopped
    ports:
      - "13378:13378"
    volumes:
      - /opt/audiobookshelf/config:/config
